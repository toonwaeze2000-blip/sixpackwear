const SUPABASE_URL='https://boaqzdyfsosswqvjgsjn.supabase.co';const SUPABASE_KEY='sb_publishable_JlneB01f5LDJ4VBO_n1E7Q_jXn5JBky';const db=window.supabase?.createClient(SUPABASE_URL,SUPABASE_KEY);const GUEST_KEY='sixpack_guest_cart';const getGuest=()=>{try{return JSON.parse(localStorage.getItem(GUEST_KEY)||'[]')}catch{return[]}};const setGuest=v=>localStorage.setItem(GUEST_KEY,JSON.stringify(v));async function currentUser(){if(!db)return null;const {data}=await db.auth.getUser();return data?.user||null}async function liveSize(productId,size){if(!db||!size)return 0;const {data}=await db.from('product_inventory').select('stock,reserved').eq('product_id',productId).eq('size',size).maybeSingle();return data?Math.max(0,Number(data.stock||0)-Number(data.reserved||0)):0}async function getCart(){const u=await currentUser();if(!u)return getGuest();const {data}=await db.from('cart_items').select('product_id,quantity,size,products(id,name,brand,category,price,image,stock,sizes,size_stock)').eq('user_id',u.id);return (data||[]).map(x=>({...x,product:x.products}))}async function addToCart(product,size='',quantity=1){if(!product?.id)throw new Error('PRODUCT_REQUIRED');const max=await liveSize(product.id,size);if(size&&max<=0)throw new Error('SIZE_OUT_OF_STOCK');if(!size&&Array.isArray(product.sizes)&&product.sizes.length)throw new Error('SIZE_REQUIRED');const limit=size?max:Number(product.stock||0);if(limit<=0)throw new Error('OUT_OF_STOCK');const u=await currentUser();if(!u){const cart=getGuest(),i=cart.findIndex(x=>x.product_id===product.id&&x.size===size);if(i>=0)cart[i].quantity=Math.min(Number(cart[i].quantity||0)+quantity,limit);else cart.push({product_id:product.id,quantity:Math.min(quantity,limit),size,product});setGuest(cart);return {guest:true}}const existing=await db.from('cart_items').select('quantity').eq('user_id',u.id).eq('product_id',product.id).eq('size',size).maybeSingle();const next=Math.min(Number(existing.data?.quantity||0)+quantity,limit);const {error}=await db.from('cart_items').upsert({user_id:u.id,product_id:product.id,quantity:next,size},{onConflict:'user_id,product_id,size'});if(error)throw error;return {guest:false}}async function removeFromCart(productId,size=''){const u=await currentUser();if(!u){setGuest(getGuest().filter(x=>!(x.product_id===productId&&x.size===size)));return}const {error}=await db.from('cart_items').delete().eq('user_id',u.id).eq('product_id',productId).eq('size',size);if(error)throw error}async function changeQty(productId,size,quantity){if(quantity<=0)return removeFromCart(productId,size);const max=await liveSize(productId,size);const next=Math.min(quantity,max);if(next<=0)return removeFromCart(productId,size);const u=await currentUser();if(!u){const c=getGuest(),i=c.findIndex(x=>x.product_id===productId&&x.size===size);if(i>=0){c[i].quantity=next;setGuest(c)}return}const {error}=await db.from('cart_items').update({quantity:next}).eq('user_id',u.id).eq('product_id',productId).eq('size',size);if(error)throw error}async function mergeGuestIntoUser(){const u=await currentUser();if(!u)return;for(const item of getGuest()){try{const max=await liveSize(item.product_id,item.size);if(max>0)await db.from('cart_items').upsert({user_id:u.id,product_id:item.product_id,quantity:Math.min(Number(item.quantity||1),max),size:item.size},{onConflict:'user_id,product_id,size'})}catch{}}setGuest([])}window.sixpackCart={getCart,addToCart,removeFromCart,changeQty,mergeGuestIntoUser};